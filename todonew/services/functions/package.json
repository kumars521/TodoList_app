{
  "name": "functions",
  "private": true,
  "type": "module",
  "main": "dist/functions/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc --watch",
    "clean": "rimraf dist",
    "start": "func start --port 7071",
    "migrate": "psql \"postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}\" -f migrations/001_initial_schema.sql"
  },
  "dependencies": {
    "@azure/functions": "^4.7.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "rimraf": "^5.0.1",
    "typescript": "^5.6.3"
  }
}
